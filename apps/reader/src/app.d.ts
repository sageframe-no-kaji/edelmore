// See https://svelte.dev/docs/kit/types#app.d.ts for information about these
// interfaces. The reader has no auth and no database in this slice — App.Locals
// stays empty until a later task wires per-request state.
declare global {
  namespace App {
    // interface Error {}
    // interface Locals {}
    // interface PageData {}
    // interface PageState {}
    // interface Platform {}
  }
}

export type {};
